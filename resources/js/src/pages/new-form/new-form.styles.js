const page = {
  display: 'flex',
  justifyContent: 'center',
};

const wrapper = {
  display: 'inline-flex',
  flexDirection: 'column',
  rowGap: '12px',
  marginTop: '12px',
};

export const styles = {
  page,
  wrapper,
};
