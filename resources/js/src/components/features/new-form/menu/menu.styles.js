const menu = {
  borderRadius: '8px',
  p: 1,
  bgcolor: 'background.paper',
  display: 'flex',
  flexDirection: 'column',
  rowGap: '8px',
  marginLeft: '10px',
};

const option = {
  opacity: '1',
};

const disabledOption = {
  opacity: '0.6',
  pointerEvents: 'none',
};

export const styles = {
  menu,
  option,
  disabledOption,
};
