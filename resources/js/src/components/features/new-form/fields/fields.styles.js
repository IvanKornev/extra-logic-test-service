const wrapper = {
  display: 'flex',
  flexDirection: 'column',
  rowGap: '10px',
};

const heading = {
  fontSize: '15px',
};

const field = {
  '& input[type="text"]': {
    fontSize: '14px',
  },
};

const select = {
  fontSize: '15px',
};

export const styles = {
  wrapper,
  heading,
  field,
  select,
};
