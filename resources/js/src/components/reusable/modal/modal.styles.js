const modal = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};

const box = {
  border: 'none',
  outline: 'none',
  p: 2,
  background: 'rgba(255, 255, 255, 0.8)',
  borderRadius: '6px',
  display: 'inline-flex',
  flexDirection: 'column',
  color: 'black',
};

export const styles = {
  modal,
  box,
};
