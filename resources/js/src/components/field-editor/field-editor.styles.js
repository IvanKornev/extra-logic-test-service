const box = {
  p: 2,
  background: 'rgba(255, 255, 255, 0.8)',
  borderRadius: '6px',
  display: 'inline-flex',
  flexDirection: 'column',
  color: 'black',
};

const modal = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
};

export const styles = {
  box, modal,
};
